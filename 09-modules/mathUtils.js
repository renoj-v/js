// A small library module — this is the "given" code for this lesson,
// like fetchValue() was in 06. You don't need to edit this file.
// It only uses NAMED exports (you can have as many of these as you want).

export const PI = 3.14159;

export function add(a, b) {
  return a + b;
}

export function multiply(a, b) {
  return a * b;
}

export function square(n) {
  return n * n;
}
